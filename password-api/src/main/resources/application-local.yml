spring:
  datasource:
    url: jdbc:mysql://localhost:3306/password?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: password
    password: password
  jpa:
    hibernate:
      ddl-auto: update

decorator:
  datasource:
    p6spy:
      enable-logging: true
      log-format: "%(executionTime)ms | %(sql)"
